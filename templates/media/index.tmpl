{{ define "media/index" }}
<!DOCTYPE html>
<html lang="en">
<head>
  <title>{{ .title }}</title>
  <link rel=icon href="{{ .icon }}">
  <link rel="stylesheet" href="{{ .style }}">
</head>
<body>
  <div id="root"></div>
  <script type="module">
    import MediaUI from {{ .main }}
    MediaUI({
      webrtc: {
        urlGroup: {{ .urlGroup }},
        iceServerURLs: {{ .iceServerURLs | ToJS }},
      }
    })
  </script>
</body>
</html>
{{ end }}